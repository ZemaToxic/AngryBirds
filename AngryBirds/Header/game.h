#pragma once
#include "utils.h"
#include "level.h"

class Game
	{
	public:
		static void slingShotStart();
		static void slingShotEnd();
		static void createLevel();
		static void init();
		static void render();
		static void update();
	};
